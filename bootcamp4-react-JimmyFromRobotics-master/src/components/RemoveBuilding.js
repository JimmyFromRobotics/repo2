import React from 'react';

const RemoveBuilding = (props) => {

    return (
        <form>
             <button onClick={props.dataT}>remove</button>
        </form>
    );




}

export default RemoveBuilding;
